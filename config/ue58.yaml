engine:
  version: "5.8"
  # Keep machine-specific paths out of Git. Set UE_ROOT before running the
  # ingestion pipeline, or replace this empty value in a local checkout.
  root: ""

paths:
  engine_source: "Engine/Source"
  engine_plugins: "Engine/Plugins"

data:
  raw: "data/raw"
  parsed: "data/parsed"
  chunks: "data/chunks"
  benchmark: "data/benchmark"
